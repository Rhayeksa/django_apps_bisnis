# from django.db import models

# from .kontak import Kontak


# class Phone(models.Model):
#     kontak = models.ForeignKey(to=Kontak, on_delete=models.CASCADE)
#     number = models.CharField(max_length=20)
